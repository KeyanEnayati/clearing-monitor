# University-specific custom parsers go in this package.
# Each file must be named after the university key in config.py (lowercase).
# Each file must expose:  def extract(soup: BeautifulSoup) -> list[dict]
